// Relationship API module - placeholder for future direct relationship operations
// Currently, relationships are accessed through resource endpoints:
// - GET /resources/{uid}/relationships
// - GET /resources/{uid}/graph
export {};
